/**
 * This mock of react-native-push-notification is incomplete!
 * Add any other functions we use from this module
 * here as 'jest.fn()' as needed!
 */
const localNotification = jest.fn();
const localNotificationSchedule = jest.fn();

export default {
  localNotification,
  localNotificationSchedule,
};
